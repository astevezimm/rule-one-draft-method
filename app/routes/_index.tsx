export default function StartPage() {
  // Page only shown to admin
  // Player names 3 to 8
  // included races: base, keleres, pok, ds, ds+
  // number of races to keep in pool
  // integrate with https://keeganw.github.io/ti4/: names(default to Map 1, Map 2, etc.) and urls for map choices
  // start draft button
  
  return "hello world"
}
